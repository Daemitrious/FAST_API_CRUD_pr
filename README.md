This repository contains two versions of the application organized into separate folders: a standalone version and a Dockerized version. The standalone version is intended for local development and can be run directly using a Python environment and Node.js, while the Dockerized version provides a containerized setup that ensures a consistent and reproducible environment across different systems. This structure allows demonstrating both traditional local execution and modern container-based deployment approaches.


